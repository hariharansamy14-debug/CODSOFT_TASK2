# --- Core web framework ---
Flask==3.0.3                   # the web framework that handles HTTP requests/responses
Flask-Cors==4.0.1              # allows the React frontend (different port/origin) to call this API
Flask-Limiter==3.8.0           # rate limiting, e.g. "max 5 login attempts per minute"

# --- Database ---
Flask-SQLAlchemy==3.1.1        # ORM: lets us write Python classes instead of raw SQL
Flask-Migrate==4.0.7           # version-controls database schema changes (like Git, but for the DB)
PyMySQL==1.1.1                 # pure-Python MySQL driver used by SQLAlchemy

# --- Authentication & Security ---
Flask-JWT-Extended==4.6.0      # issues and verifies JWT access/refresh tokens
bcrypt==4.2.0                  # one-way password hashing (never store raw passwords)
Flask-WTF==1.2.2               # provides CSRF token generation/validation

# --- File handling & data processing ---
pandas==2.2.2                  # reads/parses CSV, Excel, JSON, TXT tabular data
openpyxl==3.1.5                # Excel (.xlsx) read/write support used by pandas
python-dotenv==1.0.1           # loads secrets/config from a .env file instead of hardcoding them

# --- Duplicate detection algorithms ---
python-Levenshtein==0.26.1     # fast C implementation of edit-distance (fuzzy matching)
jellyfish==1.1.0               # Soundex / Metaphone phonetic matching algorithms
scikit-learn==1.5.2            # TF-IDF vectors + cosine similarity for text comparison

# --- Reports ---
reportlab==4.2.5               # generates PDF reports
XlsxWriter==3.2.0              # generates Excel reports with formatting/charts

# --- Background processing & caching ---
celery==5.4.0                  # runs deduplication jobs in the background (large files won't block the API)
redis==5.1.1                   # message broker for Celery + response caching

# --- Testing ---
pytest==8.3.3
pytest-flask==1.3.0
pytest-cov==5.0.0               # test coverage reporting

# --- Production server ---
gunicorn==23.0.0                # production-grade WSGI server (Flask's built-in server is dev-only)
